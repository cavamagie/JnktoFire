import groovy.json.JsonOutput
pipeline {
    agent {
docker { image 'node:params.Images' }
}
    stages {
        stage('Vars') {
          steps {
             cleanWs()
            script {
              writeFile file: 'params.json', text: JsonOutput.toJson(params)
              echo sh(returnStdout: true, script: 'env')

            }
          }
        }
       stage('CheckVars') {
   
            steps {
                sh 'pwd'             
                sh "ls -latr"       
            }

                }
        stage('Clone') {
  
   
            steps {
                sh 'pwd'
                sh " git clone https://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@github.ibm.com/Luigi-Molinaro/fyre-automation.git /tmp/fyre"                
                sh "ls /tmp"       
            }

                }        
       stage('Run') {

   
            steps {
                sh 'pwd'             
                sh "ls /tmp"       
                sh "ansible-playbook /tmp/fyre/fyre-automation.yaml -v -e \"@params.json\""
            }

                }
            
        stage('clean') {

   
            steps {
                sh 'pwd'

                sh " rm -rf /tmp/fyre"                

            }

                }
            }
        }
